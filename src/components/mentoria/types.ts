export type FAQItem = {
  question: string;
  answer: string;
};

export type Testimonial = {
  author: string;
  text: string;
};

export type Countdown = {
  days: number;
  hours: number;
  minutes: number;
  seconds: number;
};
